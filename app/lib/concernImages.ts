/**
 * Image for each concern card on the home page.
 *
 * Key: the concern name exactly as it appears in Shopify, but lowercase
 * (e.g. "dry skin", "scalp care"). Value: the image URL.
 * A concern without an entry here shows a plain dark card with its name.
 */
export const CONCERN_IMAGES: Record<string, string> = {
  // "dry skin": "https://cdn.shopify.com/s/files/.../dry-skin.webp",
};
